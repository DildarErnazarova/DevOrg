public class RankHandler {
    public rankPrize(Integer rank) {
        String prize='';
        //write logic to set the proper valie p
        if (rank==1){
            prize='Gold';
        } else if (rank==)
        }

    }
}