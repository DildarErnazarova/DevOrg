// Defining the ModernMaths class
public class ModernMaths {
    // Defining the printFactors method
    public static void printFactors(Integer Num) {
        System.debug('Factors of ' + Num + ' are:');
        // Looping from 1 to Num and checking if the number is a factor of Num
       
    }
}